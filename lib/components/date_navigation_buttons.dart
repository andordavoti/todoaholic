import 'package:flutter/material.dart';

class DateNavigation extends StatelessWidget {
  const DateNavigation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
